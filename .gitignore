/target
**/*.rs.bk
Cargo.lock.orig

# Local sidecar / env
.env
.env.local

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code session + linked worktrees (never commit — holds build output)
.claude/
